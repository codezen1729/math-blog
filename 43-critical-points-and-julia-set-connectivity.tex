\documentclass[11pt,a4paper]{article}
\input{styles/blog-preamble.tex}
\input{styles/notes-preamble.tex}
\graphicspath{{figures/complex-dynamics-notes/}}
\begin{document}
\section*{Critical Points and Julia-Set Connectivity}
% BLOG-CONTENT-BEGIN
Let's see how the critical points of polynomials on $\widehat{\mathbb C}$ determine the shape and structure of the Julia set. The following is a well-known fact.
\setcounter{theorem}{22}\begin{theorem}\label{cd5:components}
If $\mathcal A_0$ is an immediate basin of attraction, then $\widehat{\mathbb C}\setminus\mathcal A_0$ is either connected or has uncountably many components.
\end{theorem}
% Source page 3
We now ask when $\widehat{\mathbb C}\setminus\mathcal A_0$ is connected. This reveals the topological shape of polynomial Julia sets. Let us see the role of critical points, working in the ambient plane $\mathbb C\subset\widehat{\mathbb C}$.
\setcounter{definition}{13}\begin{definition}[Filled Julia set]
The filled Julia set is $K=\{z\in\mathbb C:\text{the orbit of }z\text{ is bounded}\}$.
\end{definition}
\subsection{Description of the filled Julia set}
For $f(z)=\sum_{k=0}^{d}a_kz^k$ of degree $d\ge2$, first normalise by $cf(z/c)$ to make $f$ monic. There is $r$ with $B_r(\infty)\subseteq\mathcal A(\infty)$, so the filled Julia set is $\mathcal A(\infty)^c$. Recall that if $J$ has interior, then $J=\widehat{\mathbb C}$, which is not the case here. Thus $J=\partial K$, and all bounded Fatou components belong to $\operatorname{Int}(K)$.

By the maximum modulus principle, $|f(z)|\le r$ for every $z\in\operatorname{Int}(K)$. Now let us see how critical points enter the picture.
% Source page 4
\setcounter{theorem}{23}\begin{theorem}\label{cd5:connectedness}
The sets $K$ and $\partial K$ are connected if and only if every critical point has a bounded orbit.
\end{theorem}
\begin{corollary}[Cantor filled Julia set]\label{cd5:cantor}
If $\operatorname{Crit}(f)\cap K=\varnothing$, then $K$ is a Cantor set.
\end{corollary}

\begin{proof}
Choose $R>0$ so large that
\[
f^{-1}(\overline{D_R(0)})\Subset D_R(0),
\qquad
K=\bigcap_{n=0}^{\infty}f^{-n}(\overline{D_R(0)}).
\]
If $\operatorname{Crit}(f)\cap K=\varnothing$, then the displayed equality gives an $m$ large enough that
\[
\operatorname{Crit}(f)\cap f^{-m}(\overline{D_R(0)})=\varnothing.
\]
Put $V=f^{-m}(D_R(0))$ and $W=f^{-(m-1)}(D_R(0))$. Then $\overline V\Subset W$ and $f\colon V\to W$ is a covering map. By Schwarz--Pick, applied componentwise to $V\hookrightarrow W$, there is $C>1$ such that every inverse branch of $f$ contracts the Poincar\'e metric of $W$ by at least $C^{-1}$. Since $V$ has finitely many components compactly contained in $W$, the maximum diameter of a component of $f^{-n}(V)$ tends to zero as $n\to\infty$. Hence
\[
K=\bigcap_{n=0}^{\infty}f^{-n}(V)
\]
is totally disconnected. Each component at one level contains components at every later level, and $\deg(f)\ge2$, so $K$ has no isolated points. Thus $K$ is a Cantor set.
\end{proof}
% BLOG-CONTENT-END
\end{document}
